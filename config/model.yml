model:
  weights: yolo26x-pose.pt   # n/s/m/l/x según hardware
  confidence: 0.45
  iou_threshold: 0.45
  tracker: bytetrack.yaml    # integrado en Ultralytics
  device: auto               # auto | cuda | mps | cpu
  half_precision: true       # FP16 en CUDA/MPS
